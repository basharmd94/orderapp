export const DASHBOARD_PATH = '/dashboard/default'; // Default dashboard route
export const API_BASE_URL = 'http://localhost:8000/api/v1'; // Update this with your API server address

const config = {
  fontFamily: `'Roboto', sans-serif`,
  borderRadius: 8
};

export default config;
